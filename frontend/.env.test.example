# E2E Test credentials — copy to .env.test and fill in real values
# These must be valid users in your local/test database

TEST_ADMIN_EMAIL=test-admin@company.com
TEST_ADMIN_PASSWORD=password123

TEST_MANAGER_EMAIL=test-manager@company.com
TEST_MANAGER_PASSWORD=password123

TEST_EMPLOYEE_EMAIL=test-employee@company.com
TEST_EMPLOYEE_PASSWORD=password123

# Override if your dev server runs on a different port
# PLAYWRIGHT_BASE_URL=http://localhost:3000
